const CUBE_SIZE = 1;

const Conf = {
    CUBE_SIZE: CUBE_SIZE,
    CUBE_ADD_Y: CUBE_SIZE / 2,

    DEFAULT_SIZE: 35,
    DEFAULT_BUILDER_SIZE: 10,

    ASSETS_PATH: 'assets/',
    BLOCKMODELS_PATH: 'assets/blockmodel/',

    CHUNK_SIZE: 8,
};

export default Conf;
